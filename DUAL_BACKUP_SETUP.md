# TradingView警报 - 双保险方案配置

## 📊 方案概述

你的Android应用已经支持**三路触发**，可以配置双保险甚至三保险！

```
                    TradingView Alert
                          ↓
        ┌─────────────────┼─────────────────┐
        ↓                 ↓                 ↓
   [方案A: 主要]      [方案B: 备份]      [方案C: 双备份]
   手机应用通知       邮件监控           邮件应用通知
   (离线/即时)       (需要电脑)         (手机在线)
        ↓                 ↓                 ↓
  NotificationListener  email_watcher   NotificationListener
  监控TV应用通知       (电脑监控)       监控邮件应用通知
        ↓                 ↓                 ↓
        └─────────────────┼─────────────────┘
                          ↓
                    AlertManager
                          ↓
            【3分钟声音+振动】
            【10分钟重复6次】
```

---

## ✅ 方案对比

| 方案 | 延迟 | 依赖 | 可靠性 | 优势 | 劣势 |
|------|------|------|--------|------|------|
| **A: TV应用通知** | **秒级** | 手机+TV应用 | ⭐⭐⭐⭐⭐ | 离线可用 | 需安装TV应用 |
| **B: 电脑邮件监控** | 10-30秒 | 电脑+网络 | ⭐⭐⭐⭐ | 不依赖手机 | 需要电脑24小时运行 |
| **C: 手机邮件通知** | 10-30秒 | 手机+邮件应用 | ⭐⭐⭐⭐ | 不需要电脑 | 手机必须在线 |

---

## 🎯 推荐配置：A + B (主要+备份)

### 为什么这样配置？

**方案A (主要)**: TradingView手机应用
- ✅ 离线可用 - 手机没网也能收到
- ✅ 延迟最低 - 秒级响应
- ✅ 最可靠 - 不依赖任何服务器

**方案B (备份)**: 电脑邮件监控
- ✅ 当手机应用出问题时备份
- ✅ 当手机没电/关机时备份
- ✅ 可以同时监控多个账号

---

## 📱 方案A: TradingView手机应用通知 (主要)

### Step 1: 安装TradingView应用

在你的Android手机上:
1. 打开Google Play Store
2. 搜索 "TradingView"
3. 安装官方TradingView应用
4. 登录你的TradingView账号

### Step 2: 开启通知权限

1. 手机设置 → 应用 → TradingView
2. 通知 → **开启所有通知**
3. 确保没有设置"请勿打扰"

### Step 3: 配置TradingView Alert

在TradingView网页或应用创建Alert:
1. 设置触发条件（如 BTCUSDT > 100000）
2. Notifications选项:
   - ✅ **Show Popup** (重要!)
   - ✅ **Push notification**
   - ✅ Send email (备用方案B)
3. 保存

### Step 4: 确认NotificationListener运行

你的Android应用已经有NotificationListener，它会自动监控：
- ✅ TradingView应用通知 (第131-133行代码)
- ✅ 检测关键词: tradingview, com.tradingview

### 测试

1. 在TradingView创建一个肯定会触发的Alert
2. 应该在几秒内听到手机3分钟警报

**优势**:
- ⚡ **延迟: 1-3秒**
- 📱 **离线可用**
- 🔋 **省电**

---

## 💻 方案B: 电脑邮件监控 (备份)

### 架构
```
TradingView Email Alert
    ↓
QQ邮箱/Gmail
    ↓
email_watcher.py (电脑监控)
    ↓
sse_server_with_push.py (:8080)
    ↓
Android AlertPollingService (:80/poll)
    ↓
AlertManager (3分钟警报)
```

### Step 1: 配置email_watcher

你已经有了email_watcher.py，检查配置:

```bash
cd C:\Users\27654\Desktop\交易\project\email_watcher
cat config.json
```

确保有TradingView相关的规则:
```json
{
  "accounts": [{
    "name": "QQ邮箱",
    "host": "imap.qq.com",
    "port": 993,
    "username": "your@qq.com",
    "password": "授权码",
    "mailbox": "INBOX",
    "rules": [{
      "name": "TradingView警报",
      "subject_contains": ["TradingView", "Alert", "警报"]
    }]
  }],
  "poll_seconds": 5
}
```

### Step 2: 启动服务

**方法1: 使用批处理文件**
```bash
双击: C:\Users\27654\Desktop\交易\project\start_webhook_server.bat
```

**方法2: 手动启动**

终端1:
```bash
cd C:\Users\27654
python sse_server_with_push.py
```

终端2:
```bash
cd C:\Users\27654\Desktop\交易\project\email_watcher
python email_watcher.py
```

### Step 3: 测试

```bash
# 测试推送链路
curl -X POST http://localhost:8080/push \
  -H "Content-Type: application/json" \
  -d "{\"subject\":\"测试警报\",\"from\":\"test\"}"
```

手机应该收到警报！

### Step 4: 配置Android应用

确认AlertPollingService配置:
- SERVER_URL: `http://10.0.0.170:80/poll`
- POLL_INTERVAL: 5秒

**优势**:
- 🖥️ 不依赖手机
- 📊 可以监控多个账号
- 📧 邮件有记录可查

---

## 📧 方案C: 手机邮件应用通知 (双备份)

如果你还想加第三层保险，可以配置手机邮件应用通知。

### 工作原理

```
TradingView Email Alert
    ↓
QQ邮箱/Gmail
    ↓
手机邮件应用 (QQ邮箱/Gmail App)
    ↓
邮件应用发送通知
    ↓
NotificationListener 监控邮件应用通知
    ↓
AlertManager (3分钟警报)
```

### Step 1: 安装并配置邮件应用

在你的Android手机:
1. 安装 QQ邮箱 或 Gmail 应用
2. 登录你的邮箱
3. 开启推送通知

### Step 2: 确认NotificationListener支持

你的Android应用已经支持监控邮件应用通知（第137-161行代码）:

支持的邮件应用:
- ✅ Gmail (com.google.android.gm)
- ✅ Outlook (com.microsoft.office.outlook)
- ✅ QQ邮箱 (com.tencent.androidqqmail)
- ✅ Samsung Email
- ✅ Yahoo Mail

关键词匹配:
- ✅ tradingview
- ✅ alert
- ✅ 警报
- ✅ 提醒
- ✅ btc/eth/usdt

### Step 3: 测试

1. 给自己的邮箱发送测试邮件
   - 主题包含: "TradingView Alert"
2. 手机收到邮件通知后
3. 应该触发3分钟警报

**优势**:
- 📱 不需要电脑
- 🌐 只需手机有网络
- 📧 邮件永久记录

**劣势**:
- 📶 手机必须在线
- ⏱️ 延迟10-30秒

---

## 🎯 推荐的TradingView Alert配置

在TradingView创建Alert时，为了覆盖所有方案:

### Notifications设置
```
✅ Show Popup           → 触发方案A (TV应用通知)
✅ Push notification    → 触发方案A
✅ Send email           → 触发方案B (电脑监控) + 方案C (手机邮件通知)
```

这样配置后，一个Alert会触发**三路通知**:
1. TradingView应用通知 (最快)
2. 邮件 → 电脑监控
3. 邮件 → 手机邮件应用

**防止重复警报**: AlertManager会自动去重，同一个Alert只会响一次！
(NotificationListener.java 第93-96行代码已处理去重)

---

## 🛡️ 防重复触发机制

你的Android应用已经实现了防重复:

```java
// NotificationListener.java 第37-38行
private final java.util.Set<String> processedNotifications = new HashSet<>();
private static final long NOTIFICATION_COOLDOWN = 60000; // 1分钟冷却
```

**工作原理**:
1. 每个通知会生成唯一ID
2. 处理后加入 `processedNotifications`
3. 1分钟内相同通知不会重复触发
4. 1分钟后自动清除，允许新的相同Alert

**这意味着**:
即使三路通知都到达，也只会触发一次3分钟警报！✅

---

## 🔧 完整配置检查清单

### 手机端 (Android应用)

- [ ] 已安装你的TradingViewAlertApp.apk
- [ ] 已开启 "通知访问权限"
- [ ] NotificationListener服务已运行
- [ ] AlertPollingService已运行 (如果使用方案B)

### TradingView应用 (方案A)

- [ ] 已安装TradingView移动应用
- [ ] 已登录TradingView账号
- [ ] 已开启应用通知权限
- [ ] 创建Alert时勾选 "Show Popup" 和 "Push notification"

### 电脑端 (方案B)

- [ ] email_watcher.py 已配置并运行
- [ ] sse_server_with_push.py 在8080端口运行
- [ ] 手机和电脑在同一网络 (10.0.0.x)
- [ ] TradingView Alert勾选 "Send email"

### 邮件应用 (方案C - 可选)

- [ ] 手机已安装QQ邮箱或Gmail应用
- [ ] 已登录邮箱
- [ ] 已开启邮件应用通知
- [ ] TradingView Alert勾选 "Send email"

---

## 🧪 测试流程

### 测试1: TradingView应用通知 (方案A)

```
1. 在TradingView应用创建一个测试Alert
2. 触发条件设为肯定会满足的（如 BTCUSDT > 1）
3. 勾选 "Show Popup"
4. 保存
5. 应该在1-3秒内听到手机3分钟警报
```

### 测试2: 电脑邮件监控 (方案B)

```bash
# 1. 确认服务运行
netstat -ano | findstr :8080  # sse_server运行中

# 2. 测试推送链路
curl -X POST http://localhost:8080/push \
  -H "Content-Type: application/json" \
  -d "{\"subject\":\"电脑监控测试\",\"from\":\"test\"}"

# 3. 手机应该收到警报
```

### 测试3: 邮件应用通知 (方案C)

```
1. 用另一个邮箱给自己发邮件
2. 主题: "TradingView Alert: BTCUSDT > 100000"
3. 手机邮件应用收到通知
4. 应该触发3分钟警报
```

---

## 💡 最佳实践

### 日常使用

**正常情况**:
- 主要依靠 **方案A** (TradingView应用通知)
- 延迟最低，离线可用

**手机没电/关机时**:
- 自动切换到 **方案B** (电脑邮件监控)
- 确保不错过重要警报

**电脑关机但手机在线时**:
- 自动使用 **方案C** (邮件应用通知)
- 或继续使用方案A

### 电脑自动启动

创建Windows任务计划，开机自动启动email_watcher和sse_server:

```xml
任务计划程序 → 创建任务
触发器: 启动时
操作: 运行 C:\Users\27654\Desktop\交易\project\start_webhook_server.bat
```

---

## 📊 方案对比总结

| 场景 | 推荐方案 | 原因 |
|------|----------|------|
| 手机在身边 | A | 最快，离线可用 |
| 手机没电 | B | 电脑监控 |
| 电脑关机 | A 或 C | TV应用通知或邮件应用 |
| 网络不稳定 | A | 离线可用 |
| 睡觉时 | B | 电脑24小时监控 |
| 出门在外 | A + C | 手机携带方便 |

---

## 🎉 完成！

配置完成后，你拥有了**三层保险**:

```
🛡️ 第一层: TradingView应用通知 (最快，离线)
🛡️ 第二层: 电脑邮件监控 (24小时，备份)
🛡️ 第三层: 邮件应用通知 (手机在线，双备份)
```

**不会错过任何重要警报！** ✅

---

## 🆘 故障排除

### 问题: 没有收到警报

**检查步骤**:

1. **检查TradingView Alert设置**
   - 是否勾选了 "Show Popup" 或 "Send email"
   - 触发条件是否正确

2. **检查手机应用**
   - NotificationListener是否开启
   - 通知访问权限是否授予

3. **检查TradingView应用**
   - 是否已安装并登录
   - 通知权限是否开启

4. **检查电脑服务** (方案B)
   - email_watcher是否运行
   - sse_server是否在8080端口
   - 网络是否连通

5. **查看日志**
   - Android应用: 使用 `adb logcat | grep TVAlert`
   - email_watcher: 查看终端输出
   - sse_server: 查看终端输出

---

祝交易顺利！📈🚀
